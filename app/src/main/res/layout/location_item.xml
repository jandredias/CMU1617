<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/InnerRelativeLayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:minHeight="50dp"
    android:padding="@dimen/AppMargin"
    android:orientation="vertical"
    android:layout_alignParentBottom="true"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/location_add_mesage_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="center_vertical"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="10dp"
        android:backgroundTint="@color/colorAccent"
        android:tint="@color/white"
        android:layout_centerVertical="true"
        app:srcCompat="@drawable/ic_add"/>

    <TextView
        android:id="@+id/location_name"
        android:layout_width="fill_parent"
        android:layout_toRightOf="@id/location_add_mesage_button"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:textSize="18sp"
        android:text="so big so big so big so big so big so big so big so big so big so big so big so big so big so big  on  lalalalalala" />


</RelativeLayout>